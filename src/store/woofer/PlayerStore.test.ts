import PlayerStore from './playerStore';

describe('store/woofer/playerStore', () => {
  let playerStore: PlayerStore = null;
  test('constructor', () => {
    playerStore = new PlayerStore(false, {});
  });

  test('setList', () => {

  })
});
